/**
 * Domain event types and event store — defines application-level events
 * (state transitions, validation results, etc.) and persists them for audit.
 */
package com.atheryon.mortgages.notification.event;
