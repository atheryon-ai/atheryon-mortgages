/**
 * Migration pipeline stages — ingestion, transformation, and batch validation.
 * Orchestrates the end-to-end flow from raw file upload to validated records.
 */
package com.atheryon.mortgages.migration.pipeline;
